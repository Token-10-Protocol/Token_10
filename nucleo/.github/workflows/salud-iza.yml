name: "🏥 Revisión Semanal de Salud IZA"

on:
  schedule:
    - cron: '0 9 * * 1'  # Lunes 9 AM
  workflow_dispatch:      # Ejecución manual

jobs:
  revisar-salud:
    runs-on: ubuntu-latest
    steps:
      - name: "🔄 Bajar código"
        uses: actions/checkout@v3
        
      - name: "🔍 Ejecutar detector"
        run: |
          cd nucleo/sistema-decoherencia
          python detector-simple.py
